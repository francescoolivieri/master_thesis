# Actor-only deployment config for PPO position tracking with fixed ray-order observations.
# Layout: agent pos/vel/yaw sin-cos (8) + goal pos (3) + 32 ray hit XY slots (64).
obs_dim: 75
action_dim: 4
hidden_layers: [512, 256, 128, 64]
activation: elu
log_std_init: 0.0
